name: events advance types
on: 
  pull_request:
    types: ['opened']
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: get code
        uses: actions/checout@v6
      - name: use node
        uses: actions/setup-node@v6
      - name: install dependencies
        run: npm ci
      - name: run build
        run: npm run build
      - name: deploy app
        run: echo "deploying app.."  
